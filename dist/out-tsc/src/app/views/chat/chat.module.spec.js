import { ChatModule } from './chat.module';
describe('ChatModule', () => {
    let chatModule;
    beforeEach(() => {
        chatModule = new ChatModule();
    });
    it('should create an instance', () => {
        expect(chatModule).toBeTruthy();
    });
});
//# sourceMappingURL=chat.module.spec.js.map