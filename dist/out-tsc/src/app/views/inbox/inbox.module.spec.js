import { InboxModule } from './inbox.module';
describe('InboxModule', () => {
    let inboxModule;
    beforeEach(() => {
        inboxModule = new InboxModule();
    });
    it('should create an instance', () => {
        expect(inboxModule).toBeTruthy();
    });
});
//# sourceMappingURL=inbox.module.spec.js.map